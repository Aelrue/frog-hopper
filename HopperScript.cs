using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HopperScript : MonoBehaviour
{
    // Start is called before the first frame update. For any code that will run as soon as script is enabled.
    // Runs once
    void Start()
    {
        gameObject.name = "Hopper";
    }

    // Update is called once per frame. Runs constantly while script is enabled
    void Update()
    {
        
    }
}
